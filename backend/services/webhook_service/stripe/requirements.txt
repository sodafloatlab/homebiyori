# =====================================
# Stripe Webhooks Lambda Dependencies
# =====================================

# AWS SDK
boto3==1.40.21

# Lambda runtime dependencies
# (homebiyori_common provided via Lambda Layer)

# Event processing (provided by homebiyori-common-layer)
# pydantic==2.11.7 provided via Lambda Layer

# JSON handling (built-in to Python 3.11+)
# json

# Note: webhook_service models/services imported via sys.path
# No additional dependencies required for Stripe event models