# Homebiyori Common Lambda Layer Requirements
# GEMINI.md準拠: 全Lambda関数で共通利用される依存関係

# AWS SDK - 全サービスで必須
boto3==1.34.0             # AWS サービス連携（DynamoDB、Parameter Store、SQS等）
botocore==1.34.0          # boto3の低レベル実装

# Web Framework - API Lambda用
fastapi==0.104.1          # メインWebフレームワーク
pydantic==2.5.0           # データバリデーション・型安全性確保

# Lambda Integration
mangum==0.17.0            # ASGI-Lambda間のアダプター

# 構造化ログ - 運用監視用
structlog==23.2.0         # 構造化ログ出力、CloudWatch統合

# HTTP Client - 内部API通信用
httpx==0.25.2             # 非同期HTTPクライアント、Lambda間通信

# 日時処理
python-dateutil==2.8.2   # タイムゾーン対応、日時計算用

# JSON処理
orjson==3.9.10           # 高速JSON処理、パフォーマンス最適化
