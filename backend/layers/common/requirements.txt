# Homebiyori Common Lambda Layer Requirements (Updated: 2025年8月基準)
# 全Lambda関数で共通利用される依存関係

# AWS SDK - 全サービスで必須 (2025年8月最新)
boto3==1.40.21            # AWS サービス連携（DynamoDB、Parameter Store、SQS等）
botocore==1.40.21         # boto3の低レベル実装

# Web Framework - API Lambda用 (2025年8月最新)
fastapi==0.116.1          # メインWebフレームワーク
pydantic==2.11.7          # データバリデーション・型安全性確保

# Lambda Integration (2025年8月最新)
mangum==0.19.0            # ASGI-Lambda間のアダプター

# 構造化ログ - 運用監視用 (2025年8月最新)
structlog==25.4.0         # 構造化ログ出力、CloudWatch統合

# HTTP Client - 内部API通信用 (2025年8月最新)
httpx==0.28.1             # 非同期HTTPクライアント、Lambda間通信

# 日時処理 (安定版維持)
python-dateutil==2.8.2   # タイムゾーン対応、日時計算用

# JSON処理 (2025年8月最新)
orjson==3.11.1            # 高速JSON処理、パフォーマンス最適化
